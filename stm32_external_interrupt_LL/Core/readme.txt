In this code an LED toggles every time we press a push button. The code is written for rising edge type so we need to use a pull down resistor as shown
in the schematic. Also, the dobounce circuit is shown which is necessary in order to mitigate the bouncing effect.
